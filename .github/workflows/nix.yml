name: Nix Flake

on:
  push:
    branches: [main]
    paths:
      - 'flake.nix'
      - 'flake.lock'
      - 'src/**'
      - 'build.zig'
      - '.github/workflows/nix.yml'
  pull_request:
    branches: [main]
    paths:
      - 'flake.nix'
      - 'flake.lock'
      - 'src/**'
      - 'build.zig'
      - '.github/workflows/nix.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  GH_TOKEN: ${{ secrets.GH_TOKEN || secrets.GITHUB_TOKEN }}

jobs:
  nix-build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          nix_path: nixpkgs=channel:nixos-24.05

      - name: Check flake
        run: nix flake check

      - name: Build wart
        run: nix build

      - name: Test wart binary
        run: |
          ./result/bin/wart --help
          ./result/bin/wart --version || echo "Version flag not supported"

      - name: Enter dev shell
        run: |
          nix develop --command zig version
